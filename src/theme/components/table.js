import { mode } from '@chakra-ui/theme-tools'
export const tabelStyles = {
    components: {
        table: {
            thead: {
                th: {
                    fontSize: '12px',
                    color: '#a0aec0',
                },
            },
        },
    },
}